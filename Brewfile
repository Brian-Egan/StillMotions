# Build-machine tooling, pinned so the generator version is reproducible.
# See docs/decisions/0006-project-generator.md.
#
#   brew bundle install
#
# Rust is NOT installed via Homebrew. Use rustup so target toolchains can be managed:
#   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
# See docs/decisions/0004-gifski-integration.md.

brew "xcodegen"
